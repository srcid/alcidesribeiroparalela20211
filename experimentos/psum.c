#include <stdio.h>
#include <omp.h>

int* prefix_sum(int* arr, int arr_size) {
    int p = 2;
    int res = (int*)calloc(arr_size, sizeof(int));

    return res;
}

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
